class Particle
{
constructor()
{
this.body = Bodies.circle(x,y, this.r,options);
this.color=color(random(0,255), random(0,255), random(0,World.add(world,this.body)));
if(frameElement%60===0)
{
    particles.push(new Particle(random(width/2-10, width2+10), 10,10));
}
}
display()
{
    var particles = []
    var plinkos = []
    var divisions = []
}
}
